{
  "scripts": {
    "build:all": "pnpm build:projects && pnpm build:inject",
    "build:projects": "pnpm -r build",
    "build:inject": "pnpm run build:inject --dir scripts/build",
    "preview": "serve dist",
    "pretty": "prettier -w **/{src,scripts}/** --plugin-search-dir=."
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "prettier-plugin-organize-imports": "^2.3.4",
    "serve": "^13.0.2",
    "typescript": "^4.5.4"
  }
}
